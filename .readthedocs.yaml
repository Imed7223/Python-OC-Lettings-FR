version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.11" # Stable pour Read the Docs

python:
  install:
    - requirements: requirements.txt
    - method: pip

sphinx:
  configuration: docs/conf.py # Vérifie bien que ton conf.py est dans un dossier "docs"
